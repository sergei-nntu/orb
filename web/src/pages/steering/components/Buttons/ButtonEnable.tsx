import React from 'react';

export type PoseProps = {
    buttonName: string | undefined;
};

// export default function ButtonEnable(props: buttonName) {
//     const { buttonName } = props;
//     return <button>{buttonName}</button>;
// }

export default function ButtonEnable() {
    // const { buttonName } = props;
    return <button>enable stepper</button>;
}
